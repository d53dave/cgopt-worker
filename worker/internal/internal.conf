{
    optimization {
        defaults {
            max_steps = 100000
            initial_temp = 1000
            threads_per_block = 32
            blocks_per_grid = 32
        }
    }
    tasks {
        worker {
            prefix = 'worker$$'
        }
    }
}