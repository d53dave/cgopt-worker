{
    optimization {
        defaults {
            max_steps = 100000
            initial_temp = 1000
            threads_per_block = 32
            blocks_per_grid = 32
        }
    }
    worker {
        should_run = true
    }
    cuda {
        template_path = worker/cuda/cuda.template.py
    }
    tasks {
        worker {
            prefix = worker@
        }
        stats {
            prefix = stats@
        }
        ping {
            prefix = ping@
        }
    }
}